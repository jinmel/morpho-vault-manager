#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
# shellcheck source=../common.sh
source "$SCRIPT_DIR/../common.sh"

log "resetting repo-local temporary harness state"
rm -rf "$ROOT_DIR/.tmp"
mkdir -p "$ROOT_DIR/.tmp"
log "reset complete"
